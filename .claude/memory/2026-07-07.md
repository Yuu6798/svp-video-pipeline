# Session Summary — 2026-07-07

### Context

svp-video-pipeline に開発規律インフラを移植した。移植元は
ugh-prompt-engine（参照専用・変更禁止）。対象は discipline テスト一式
（`tests/discipline/`）、`.claude/memory/` 骨格（`_index.md` /
`STATUS.md` / `archive/INDEX.md`）、`.claude/briefs/_index.md`、
`.claude/skills/{wrap-up,new-brief}/SKILL.md`、`AGENTS.md`、
`.claude/settings.json` + SessionStart hook、ルート `CLAUDE.md` の
Advisor Strategy / Workflow / 終了時ルール / PR 本文規約の改訂。

### Design Decisions

- **discipline テストの配置**: svp-video-pipeline はネスト構成（ルートに
  CLAUDE.md、`svp_pipeline/` 配下に pyproject.toml/src/tests）のため、
  discipline テストを `svp_pipeline/tests/discipline/` に置き、CI の
  working-directory 設定（`svp_pipeline/` 内で `pytest tests/ -q`）を
  無改修で通す設計にした。`_helpers.py` の `REPO_ROOT` は
  `parents[3]`（svp_pipeline/tests/discipline/ から 3 階層上）でリポジトリ
  ルートを指す。
- **README 350 行キャップの新設**: 移植元にはない新規テスト
  `test_readme_line_cap.py` を追加し、ルート `README.md` と
  `svp_pipeline/README.md` の両方を 350 行 hard limit で CI 対象化した
  （CLAUDE.md の README 管理ポリシーを初めて実行可能な形で enforce）。
  `svp_pipeline/README.md` が現時点で 343 行と hard limit 目前だったため、
  STATUS.md の Next-Issue Queue に分離タスクを P2 でシードした。
- **スコープ外項目の記録**: 型チェッカー導入、CI markdown-lint の実質化、
  テスト規約（モック不使用推奨）と実態（`tests/fixtures/mock_*.py`）の
  乖離は本移植のスコープ外と判断し、STATUS.md の Next-Issue Queue に
  P3 として記録した（次セッションでの着手判断に委ねる）。
- **AGENTS.md / skills の適応**: ugh 固有のロードマップ参照
  （roadmap_goal1 等）や CompositionScore/PhysicalLayer 固有チェックは
  svp 固有の対象（SVP schema `schema/svp.py` 五層、バックエンド
  インターフェース `generator/image_base.py`、`tests/fixtures/` の mock
  バックエンド、`samples/` の SVP JSON）に置換した。

### Artifacts

- `svp_pipeline/tests/discipline/`: `__init__.py`, `_helpers.py`, 4 件の
  移植テスト（CLAUDE.md line cap / _index.md compactness / STATUS.md next
  queue / STATUS.md phase paragraph）+ 新設 `test_readme_line_cap.py`、
  `fixtures/` 4 ファイル、`README.md`
- `.claude/memory/`: `_index.md`, `2026-07-07.md`（本ファイル）,
  `STATUS.md`, `archive/INDEX.md`
- `.claude/briefs/_index.md`（空シード）
- `.claude/skills/wrap-up/SKILL.md`, `.claude/skills/new-brief/SKILL.md`
- `AGENTS.md`（ルート新設）
- `.claude/settings.json`, `.claude/hooks/session-start.sh`
- ルート `CLAUDE.md` 改訂（Advisor Strategy 節置換、Workflow 節新設、
  終了時ルール / Pull Request 節更新、Testing 節に discipline テスト言及追記）
- `.gitignore` に `.claude/settings.local.json` / `.claude/worktrees/` 追記

### Handoff

- discipline テスト・全件テスト・ruff の実行結果は
  scratchpad の `ruff.log` / `discipline.log` / `pytest_full.log` /
  `line_counts.log` / `git_status.log` を参照（本セッションの Design
  Memo §検証 手順で生成）。
- コミットは本セッションでは行っていない（Design Memo の指示によりレビュー
  後に別途実施）。
- 次セッション以降、STATUS.md Next-Issue Queue の README-1（README 350
  行 hard limit 目前の分離作業）を優先的に検討すること。

## Session 2

### Context

確定済み Design Memo に基づく技術負債改善を実装した。監査対象は
`svp_pipeline/src/svp_pipeline/` 全体（コード側監査: debt_src.md）と
CI/pyproject/README/CLAUDE.md/STATUS.md（インフラ側監査: debt_infra.md）。
Session 1 で移植した discipline インフラが検出した README-1（343 行、
hard limit 350 行目前）が主動機。ブランチ
`claude/dev-discipline-migration-i0yhe3` 上で継続実施（コミットは未実施）。

### Design Decisions

- **docs/ 3 本新設**: `docs/cli.md`（Usage + CLI Options 全 121 行を原文移設）、
  `docs/architecture.md`（Architecture 全 85 行）、`docs/backend_comparison.md`
  （日付付き実験データ）。`svp_pipeline/README.md` は 343→135 行に圧縮し、
  各 section にリンク + 要約のみ残した。全 section 30 行以内を確認済み。
- **タイムスタンプ統一**: `utils/timestamps.py` を新設し
  `utc_now_iso()`/`utc_now_compact()` を提供。`pipeline.py`（timestamp +
  run_dir 命名）・`semantic/repair.py`・`semantic/image_audit.py`・
  `tools/archive_to_drive.py`（ローカル `_utc_now_iso` 定義を削除）の
  4 箇所を統一実装に置換。ローカル時刻依存だったテストは無かったため
  挙動変更の副作用なし。
  - **エラーハンドリング**: `image_openai.py` の `openai` ハード依存
  try/except ガードと `pipeline.py` の `pillow` ハード依存ガードを除去し
  素の import に変更（到達不能な `TypeError` 潜在バグの解消）。
  `composite.py` の重複 `_extract_png_bytes`/`_raise_openai_image_error`
  を削除し、既存パターン（`_resolve_size`/`_build_reference_file` の
  private 直呼び）を踏襲して `OpenAIImageBackend` インスタンスの同等
  メソッドへ委譲。`pipeline.py:488,560` / `cli.py:1073,1122` の
  制御フロー用 `assert` を `RuntimeError` 送出に置換。
- **pyproject/CI**: dev extras に `pytest-cov`、ruff select に `FA` を追加し
  `__init__.py` 8 本に future import を追加（ruff FA 0 件確認）。
  `addopts = "-ra --strict-markers"` 追加（カスタムマーカー未使用のため
  安全）。CI の `markdown-lint` ジョブを `readme-check` に改称しルート/
  `svp_pipeline/` 両 README を対象化、`concurrency` ブロック・
  `timeout-minutes`（readme-check 5 / test 20）・push 限定
  `paths-ignore: ['.claude/memory/**']` を追加。
- **CLAUDE.md 文言同期**: Testing 節を mock 前提の実態（TEST-1 相当の
  乖離）に合わせ修正、Style 節を PEP 604/585 表記に修正（旧
  `Optional/List/Dict` 表記は ruff UP と矛盾していた）、Patterns 節の
  クランプ規約に `np.clip` 等の注記追加、「## 設計ドキュメント索引」表を
  新設（227 行、400 行 cap 内）。
- **STATUS.md**: 解消済みの README-1/CI-1/TEST-1 を Next-Issue Queue から
  削除、Phase 段落に本セッションの実施内容を追記（単一段落維持）。
  debt_src.md/debt_infra.md の残 findings から新規 5 件
  （PLANNER-1/TEST-2/DEP-1/ENV-1/LOG-1）を Queue にシードした（TYPE-1 は
  継続）。

### Artifacts

- 新設: `docs/cli.md`, `docs/architecture.md`, `docs/backend_comparison.md`,
  `svp_pipeline/src/svp_pipeline/utils/timestamps.py`
- 変更: `svp_pipeline/README.md`（343→135 行）, ルート `README.md`
  （GEMINI_API_KEY 代替別名の注記）, `CLAUDE.md`, `svp_pipeline/pyproject.toml`,
  `.github/workflows/ci.yml`, `.claude/memory/STATUS.md`,
  `svp_pipeline/src/svp_pipeline/{pipeline.py, cli.py,
  generator/{image_openai.py, composite.py},
  semantic/{repair.py, image_audit.py}, tools/archive_to_drive.py,
  __init__.py, presets/__init__.py, presets/failure/__init__.py,
  prompts/__init__.py, schema/__init__.py, semantic/__init__.py,
  tools/__init__.py, utils/__init__.py}`
- 検証ログ: scratchpad の `ruff2.log` / `pytest_full2.log` /
  `line_counts2.log` / `git_status2.log`

### Handoff

- コミットは本セッションでも行っていない（CLAUDE.md 規約によりレビュー後に
  別途実施）。
- 次セッションで STATUS.md Next-Issue Queue の PLANNER-1（P2、最優先）を
  検討すること。TYPE-1/TEST-2/DEP-1/ENV-1/LOG-1 は P3。
